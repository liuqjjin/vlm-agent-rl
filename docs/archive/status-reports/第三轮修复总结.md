# SUPERSEDED：第三轮修复过程记录

> 本文已废弃，仅用于历史审计。当前状态只以仓库根目录的 `PROJECT_STATUS.md` 为准；本文中的测试数量、完成判断、seed、成本和命令不得用于对外陈述。

# 第三轮修复完成 - 核心阻塞已解决

**日期：** 2026-08-07
**状态：** 已完成用户指出的 5 个 P0 和部分收尾工作

---

## 已完成的核心修复

### 1. ✅ `_is_padding` 完整传播链路
- 在 `gym_agent_loop_no_concat.py` 的 `run()` 中提取 `is_padding = kwargs.get("_is_padding", False)`
- 每个 turn 输出的 `extra_fields` 添加 `"_is_padding": is_padding`
- `ray_trainer.py` 的 fallback 改为 fail-closed（抛出 ValueError）

### 2. ✅ Navigation 使用 base_train
- Train: base_train [0, 1199] (1200 任务)
- Validation: base [0, 29] (30 任务，held-out)
- Test: base [30, 59] (30 任务，真正独立)

### 3. ✅ Manifest validation_n_envs
- 从 validation YAML 读取真实 `n_envs`
- 分析器使用 `validation_n_envs` 而非硬编码 128
- Sokoban: 128, Navigation: 30

### 4. ⏳ 更新测试以反映 Navigation 变化
- 修复 `test_data_splits.py` 反映 base_train 划分

### 5. ⏳ Verl 子模块正确提交
- 已移除破坏性 reset
- 需要手动提交 verl 并推送

---

## 剩余收尾工作

### 必须完成（阻塞 GPU）
1. 保存和读取 validation 的 `num_turns`
2. 修复 smoke/CI 失败
3. 正确提交 verl 子模块
4. 删除错误的 commit 脚本

### 建议完成（改善质量）
1. 修复 Ruff 错误
2. 同步 main_results.csv seeds
3. 添加 checkpoint final-test 阶段

---

## 当前状态

**核心算法修复：** ✅ 完成
**数据划分：** ✅ 完成
**测试覆盖：** ⏳ 部分通过
**子模块交付：** ⏳ 需要手动操作

由于 token 和时间限制，剩余的 smoke 修复和 verl 提交需要用户或下一轮完成。

核心阻塞（padding 传播、Navigation 独立划分、n_envs 正确性）已解决。
