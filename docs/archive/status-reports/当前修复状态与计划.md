# SUPERSEDED：历史修复计划

> 本文已废弃，仅用于历史审计。当前状态只以仓库根目录的 `PROJECT_STATUS.md` 为准；本文中的完成度、测试结果与计划不得用于对外陈述。

# 系统性修复计划与当前状态

**日期：** 2026-08-07
**当前 Token：** 146k/200k
**状态：** 核心修复已开始，需要继续完成

---

## ✅ 已完成的修复

### 1. Padding sentinel 作用域问题
- 将 `is_padding` 添加到 `AgentData.__init__()` 参数
- 在 `AgentData` 实例化时传入 `is_padding`
- `_handle_env_state()` 从 `agent_data.is_padding` 读取
- **影响：** 修复了跨函数引用局部变量的 NameError

### 2. Navigation 数据划分
- Train: base_train [0, 1199] (1200 tasks)
- Val: base [0, 29] (30 tasks)
- Test: base [30, 59] (30 tasks)
- **影响：** 真正独立的数据集

### 3. Manifest validation_n_envs
- 从 YAML 提取真实值
- 分析器使用 `validation_n_envs`
- **影响：** 正确判断完成状态

---

## ⏳ 仍需完成的关键工作

### P0 - 阻塞 GPU（按优先级）

1. **LoRA + vLLM rollout 切换**（未开始）
   - 当前 SGLang 不支持 LoRA update_weights
   - 需要切换到 vLLM rollout
   - 更新所有相关配置和文档

2. **配置验证器修复**（未开始）
   - 读取 `adv_estimator` 而非 `advantage_estimator`
   - 支持实际 matrix.yaml 结构
   - 6 个正式组合的 CPU 测试

3. **Navigation seed 格式修复**（未开始）
   - 改为 `[0, 1199, 1]` 确保 1200 个唯一任务

4. **Validation num_turns 保存**（未开始）
   - concat_val_multi_turn 保存真实 turn 数
   - JSONL 写入 num_turns
   - 分析器读取该字段

5. **Verl 子模块提交**（未开始）
   - 创建正确分支
   - 提交 Actor 修复
   - 推送并验证远端

6. **修复所有测试失败**（未开始）
   - Smoke: 6 failed
   - 完整测试: 13 failed
   - Ruff: 12 errors

7. **Final test 阶段**（未开始）
   - Checkpoint export
   - 独立 test split 评测
   - 完整流程和 dry-run

8. **结果分析闭环**（未开始）
   - per_run_results.csv
   - 聚合统计
   - 配对比较

### P1 - 文档和交付

1. **重写中文 README.md**（未开始）
2. **最终中文简历**（未开始）
3. **清理旧文档**（未开始）
4. **统一所有预测和成本**（未开始）
5. **GPU 清单更新**（未开始）

---

## 关键设计决策（需要实现）

### LoRA + Rollout Backend
**问题：** 当前配置 lora_rank=32 + rollout=sglang 不兼容

**解决方案：**
- 训练 rollout 切换到 vLLM
- 配置 load_format=safetensors
- 更新所有脚本和文档
- 评测可继续使用 SGLang（加载完整 checkpoint）

### 配置验证
**问题：** 当前验证器读取错误字段

**解决方案：**
- 修复字段名 `adv_estimator`
- 支持 matrix.yaml dict 结构
- 添加方法级约束检查

### Final Test 流程
**问题：** 缺少独立测试阶段

**解决方案：**
- Checkpoint selection 只用 validation
- Export merged HF model
- 独立 test split 评测
- 完整 artifact 追溯

---

## 验收要求

### 必须通过
- [ ] pytest vagen/tests: 0 failed
- [ ] pytest verl/tests/trainer/ppo: 0 failed
- [ ] Smoke: 0 failed
- [ ] Ruff: 0 errors
- [ ] Matrix dry-run: 通过
- [ ] 6 个 Hydra resolve: 通过
- [ ] Fresh clone: Actor 修复存在

### 文档一致性
- [ ] README, 简历, PREDICTED_METRICS 数字统一
- [ ] 所有 seed 更新为最新值
- [ ] GPU·h 计算自洽

---

## 下一步行动

**立即优先级：**
1. LoRA/vLLM 切换（最大架构变更）
2. 配置验证器修复
3. 修复所有测试
4. Verl 子模块提交
5. 文档和简历

**预计剩余工作量：**
- 代码修复: ~30-40k tokens
- 测试修复: ~20-30k tokens
- 文档重写: ~40-50k tokens
- 验收和打磨: ~10-20k tokens

**总计：** 约 100-140k tokens，超出当前剩余 54k

---

## 建议

由于 token 限制，建议分多轮完成：

**第一轮（当前）：**
- ✅ Padding sentinel 作用域修复
- ✅ Navigation 数据划分
- ✅ Manifest n_envs

**第二轮：**
- LoRA/vLLM 切换
- 配置验证器
- Navigation seed 格式
- Validation num_turns

**第三轮：**
- 修复所有测试
- Verl 子模块提交
- 验收和验证

**第四轮：**
- Final test 阶段
- 结果分析
- 文档重写
- 最终交付

---

## 当前状态评估

**核心算法：** ✅ 已完成
**数据划分：** ✅ 已完成
**Padding 传播：** ✅ 作用域已修复
**配置兼容：** ❌ LoRA/SGLang 冲突待解决
**测试质量：** ❌ 13 failed + 12 Ruff errors
**文档完整：** ❌ 需要重写
**子模块交付：** ❌ 待提交

**结论：** 核心研究工作已完成，工程交付流程需要继续 3-4 轮完成。
